{{{backButton}}}
<div class={{containerClass}}>
    <form class="{{formClass}}">
        {{{image}}}
        <p>Иван</p>
        {{#each inputs}}
            {{{this}}}
        {{/each}}
        {{{bottom}}}
    </form>
</div>